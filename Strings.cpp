#include <iostream>
#include <string>
using namespace std;

int main() {
       string a,b,c;
       string e;
       cin>>a>>b;
       cout<<a.length()<<" "<<b.length()<<endl;
       cout<<a+b<<endl;
       c[0]= a[0];
       a[0]=b[0];
       b[0]=c[0];
       cout<<a<<" "<<b;  
    return 0;
}
